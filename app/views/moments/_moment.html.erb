<%= react_component(
      "MomentCard",
      props: {
        items: @moments,
        item: moment,
        date: ::TimeAgo.created_or_edited(moment)
      },
      html_options: {
        class: "moment"
      }
    )
%>
